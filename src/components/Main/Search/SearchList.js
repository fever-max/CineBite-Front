import React from 'react';

const SearchList = () => {
           // 검색어 목록 가져오기
        //    const fetchSavedKeywords = async () => {
        //     try {
        //       const keywords = await getSavedSearchKeywords(); // 백엔드에서 검색어 목록 가져오기
        //       setSavedKeywords(keywords);
        //     } catch (error) {
        //       console.error('검색어 목록을 가져오는 중 오류가 발생했습니다:', error);
        //     }
        //   }
    return (
        <div>
            {/* 최근검색 리스트 */}

 
        </div>
    );
};

export default SearchList;